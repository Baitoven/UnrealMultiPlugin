#include "UIContainer.h"

void UUIContainer::DisplayMenuUI_Implementation()
{
}

void UUIContainer::DisplayJoinListUI_Implementation()
{
}

bool UUIContainer::Initialize()
{
	return false;
}

void UUIContainer::OnLevelRemovedFromWorld(ULevel* InLevel, UWorld* InWorld)
{
	MenuTearDown();
	Super::OnLevelRemovedFromWorld(InLevel, InWorld);
}

void UUIContainer::MenuTearDown()
{
	RemoveFromParent();
	UWorld* World = GetWorld();
	if (World)
	{
		APlayerController* PlayerController = World->GetFirstPlayerController();
		if (PlayerController)
		{
			FInputModeGameOnly InputModeData;
			PlayerController->SetInputMode(InputModeData);
			PlayerController->SetShowMouseCursor(false);
		}
	}
}
